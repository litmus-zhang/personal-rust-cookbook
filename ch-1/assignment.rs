use std::io::stdin;
use std::{f32, f64, i16, i32, i64, i8, isize, u16, u32, u64, u8, usize};

fn main() {
    let age: i32 = 40;
    println!("AGe is {}", age);
    println!("Max i32 is {}", i32::MAX);
    println!("Min i32 is {}", i32::MIN);
    let (f_name, l_name) = ("Litmus", "Zhang");
    println!("First name {0}, and last name {1}", f_name, l_name);
}
